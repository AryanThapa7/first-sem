#include <stdio.h>
#include <stdlib.h>

/* free () 
this function is used to deallocate memory that was previously allocated by malloc, calloc, or realloc. It takes a pointer as an argument and frees the memory pointed to by that pointer. After calling free(), the pointer becomes invalid and should not be used again until it is assigned a new value.
*/